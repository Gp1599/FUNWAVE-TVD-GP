4.                  imports the numpy library for creating and processing arrays
5.                  imports the matplotlib.pyplot library for visualizing beach 2D data 
6.                  imports the os library for creating paths to necessary directories/files more efficiently

9, 10, or 11.       initializes the directory path to beach2D radiation output files

13.                 loads eta
16.                 reading n and m from the eta's shape (n -> Nglob from input file & m -> Mglob from input file)
17.                 initializes dx used for plot location and probably discretization
18.                 initializes dy used for plot location and probably discretization

20.                 initializes x as a 1D array from m through plot location & discretization
21.                 initializes y as a 1D array from n through plot location & discretization

24.                 initializes the x coordinates of the sponge area (related to the Sponge Layer input variables)
25.                 initializes the y coordinates of the sponge area (related to the Sponge Layer input variables)

27.                 initializes the x coordinates of the wavemaker area (related to the Wavemaker layer)
28.                 initialized the y coordinates of the wavemaker area (related to the Wavemaker layer)

31.                 indicates the list of files, via file number references, to test
32.                 indicates the parallel array for files to contain their respective minimum times

35.                 initializes the plot's width hyperparameter
36.                 initializes the plot's length hyperparameter
40.                 initializes the main figure using these hyperparameters along with the dpi value

for every subplot
43.                 initializes fnum to be used to help read eta's filename
44.                 extracts the eta data from the current eta file
45.                 extracts mask data from the current eta file

47.                 creates the masked copy of the eta matrix

49.                 creates and adding the subplot 
50.                 gives space between subplots with hspace = 1 and wspace = 0.25
51.                 gives the current subplot a coolwarm background based on the masked copy of the eta matrix

53.                 initializes the current subplot's title with "Time = <time> sec" where <time> is its corresponding minimum time
54.                 applies the title to the current subplot
55.                 holds the current subplot (depreciated and commented out)

58.                 draws the sponge's area with the color green
59.                 gives the sponge's area the name "Sponge" to be colored green and rotated 90 degrees

61.                 draws the wavemaker's area with the color "k"
62.                 gives the wavemaker's area the name "Wavemaker" to be colored "k" and rotated 90 degrees

65.                 if the current subplot is the first, it initializes the y-axis label to "Y (m)"
66.                 if the current subplot is the first, it initializes the x-axis label to "X (m)"
68.                 if the current subplot is not the first, it initializes the x-axis label to "X (m)"
69.                 if the current subplot is not the first, it initializes the subplot's colorbar
70.                 if the current subplot is not the first, it changes the subplot's colorbar label to "'$\eta$' + ' (m)' and rotates the colorbar 90 degrees
end of for

74.                 publishes the main figure as eta_wave_2d.png